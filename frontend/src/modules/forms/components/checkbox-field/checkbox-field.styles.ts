import { roqMakeStyles } from 'modules/common/utils';

export const useCheckboxFieldStyles = roqMakeStyles(() => ({
  formControl: {
    borderBottom: '1px solid #949494',
    paddingBottom: 6,
  },
}));
